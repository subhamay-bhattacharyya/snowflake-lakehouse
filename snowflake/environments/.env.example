# Environment Configuration Template
# Copy this file and rename to match your environment (e.g., dev.env, prod.env)
# Add actual values and keep the file out of version control

# AWS Configuration
AWS_ROLE_ARN=arn:aws:iam::123456789012:role/snowflake-s3-access-role
S3_BUCKET_NAME=my-lakehouse-bucket
AWS_REGION=us-east-1

# Environment Identifier
ENVIRONMENT=dev
